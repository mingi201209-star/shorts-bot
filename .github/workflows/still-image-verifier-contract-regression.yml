name: Still Image Verifier Contract Regression

on:
  pull_request:
    paths:
      - "ci_still_image_verifier_contract_hotfix.py"
      - "ci_still_vision_evidence_groups_hotfix.py"
      - "ci_still_vision_evidence_trace_hotfix.py"
      - "ci_final_visual_semantic_qa_hotfix.py"
      - "video/still_image_fallback.py"
      - "quality/still_image_verifier_contract_regression_test.py"
      - "quality/still_image_budget_regression_test.py"
      - "quality/run_33259567582_still_vision_evidence_regression_test.py"
      - ".github/workflows/still-image-verifier-contract-regression.yml"
  workflow_dispatch:

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install focused runtime dependency
        run: python -m pip install requests openai
      - name: Run verifier contract regression
        run: python quality/still_image_verifier_contract_regression_test.py
      - name: Run verified still budget regression
        run: python quality/still_image_budget_regression_test.py
      - name: Run 33259567582 structured Vision evidence regression
        run: python quality/run_33259567582_still_vision_evidence_regression_test.py
