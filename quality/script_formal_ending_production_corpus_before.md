Script Formal-Ending Production Corpus V1 BEFORE probe is executable via:

python -m quality.script_formal_ending_production_corpus_regression_test --before

The probe intentionally loads the unpatched runner formalizer and must reproduce Run 33185606044's strict speech-style failure for `줄인다.` before the production composition installer is applied. It is not a production runtime file.
