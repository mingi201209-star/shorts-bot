# Design

The contract intentionally separates collection from interpretation. Ingestion can update evidence over time; future ranking code can compare performance without coupling Candidate Explorer to a provider API.
