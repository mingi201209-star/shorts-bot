# Lineage identity

`lineage_id` is required and should be stable for the produced-video record. Callers may compose it from their existing candidate/production identifiers as long as the value is deterministic for that lineage.
