# Provider mapping

Provider ingestion should translate upstream analytics into this contract. Downstream growth experiments should consume the normalized lineage rather than raw YouTube API response shapes.
