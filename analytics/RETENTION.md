# Retention checkpoints

Retention is stored as a provider-dependent mapping. Typical keys may include `1s`, `3s`, `5s`, `midpoint`, and `ending`. Missing checkpoints stay absent rather than being synthesized.
