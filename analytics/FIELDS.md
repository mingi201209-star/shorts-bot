# Lineage fields

Creative metadata (`candidate`, `selected_hook`, `script`) is joined to `production`, `publication`, `cost`, and delayed `performance` snapshots by `lineage_id`. `views` and `engaged_views` remain distinct fields.
