# API

- `make_performance_snapshot(...)`: validates one delayed observation.
- `make_video_lineage(...)`: creates a new lineage with pending 24h/72h snapshots.
- `normalize_video_lineage(record)`: adds current contract defaults to legacy records.
